<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="{{ path('admin_pins_index') }}">Panterest</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    {% if is_granted("ROLE_ADMIN") %}
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('admin_pins_index') }}">Dashboard <span class="sr-only">(current)</span></a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                         {% if not app.user.verified %}
                             <div class=" alert alert-danger  p-1 mt-1 mb-0"> Account Not Verified Please Check You Mail</div>
                        {% endif %}
                    </li>

                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('pins_index') }}"> Pins <span class="sr-only">(current)</span></a>
                </li>
                    {% if   app.user.verified%}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('create_pin') }}">New</a>
                        </li>
                    {% endif %}

                <li class="nav-item">
                    <a class="nav-link" href="#">Account</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Log out</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}">Create Account</a>
                </li>
                {% endif %}


            </ul>

        </div>
    </div>

</nav>