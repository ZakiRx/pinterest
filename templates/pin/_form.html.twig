{{ form_start(form) }}
<input type="hidden" name="_token" value="{{ csrf_token("pin"~pin.id) }}">
{{ form_rest(form) }}
<button type="submit" class="btn btn-primary">{{ textButton }}</button>
{{ form_end(form) }}